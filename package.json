{
  "name": "mybrandos",
  "private": true,
  "version": "0.1.0",
  "description": "mybrandOS — Gateway to a person's Digital Life",
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently -n api,web -c teal,gold \"npm:dev:api\" \"npm:dev:web\"",
    "dev:api": "npm run dev -w @mybrandos/api",
    "dev:web": "npm run dev -w @mybrandos/web",
    "build": "npm run build -w @mybrandos/shared && npm run build -w @mybrandos/integrations && npm run db:generate && npm run build -w @mybrandos/api && npm run build -w @mybrandos/web",
    "build:api": "npm run build -w @mybrandos/shared && npm run build -w @mybrandos/integrations && npm run db:generate && npm run build -w @mybrandos/api",
    "start": "mkdir -p apps/api/data && npm run db:push && npm run start -w @mybrandos/api",
    "db:generate": "npm run db:generate -w @mybrandos/api",
    "db:push": "npm run db:push -w @mybrandos/api",
    "db:seed": "npm run db:seed -w @mybrandos/api",
    "setup": "npm install && npm run build -w @mybrandos/shared && npm run build -w @mybrandos/integrations && npm run db:generate && npm run db:push && npm run db:seed",
    "typecheck": "npm run build -w @mybrandos/shared && npm run build -w @mybrandos/integrations && npm run typecheck -w @mybrandos/api",
    "test": "npm run test -w @mybrandos/api"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  },
  "engines": {
    "node": ">=20"
  }
}
